image: rust:1.36-stretch

variables:
  CARGO_HOME: $CI_PROJECT_DIR/cargo

stages:
  - build
  - publish

Build:
  stage: build
  before_script:
  - rustc --version
  - cargo --version
  script:
  - make release
  - make luxtorpeda.tar.xz
  artifacts:
    paths:
    - luxtorpeda.tar.xz

pages:
  stage: publish
  script:
  - mkdir public
  - cp luxtorpeda.tar.xz public/
  artifacts:
    paths:
    - public
  only:
  - master

cache:
  paths:
    - cargo/
    - target/
